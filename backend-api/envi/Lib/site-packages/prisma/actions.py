# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class FactActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Fact]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Fact.prisma().query_raw(
            'SELECT * FROM Fact WHERE Fact_id = $1',
            'bbadfchfja',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Fact
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Fact.prisma().query_first(
            'SELECT * FROM Fact WHERE Fact = $1',
            'dhheabfhf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.FactCreateInput,
        include: Optional[types.FactInclude] = None
    ) -> _PrismaModelT:
        """Create a new Fact record.

        Parameters
        ----------
        data
            Fact record data
        include
            Specifies which relations should be loaded on the returned Fact model

        Returns
        -------
        prisma.models.Fact
            The created Fact record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Fact record from just the required fields
        fact = Fact.prisma().create(
            data={
                # data to create a Fact record
                'Fact_id': 'ggciceaie',
                'Fact': 'bbehjachib',
                'num_views': 2035015474,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.FactCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Fact records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Fact record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Fact.prisma().create_many(
            data=[
                {
                    # data to create a Fact record
                    'Fact_id': 'dgiiaaijj',
                    'Fact': 'bfaiacjjfc',
                    'num_views': 486256185,
                },
                {
                    # data to create a Fact record
                    'Fact_id': 'bagcfbhiig',
                    'Fact': 'cghideieh',
                    'num_views': 180171308,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.FactWhereUniqueInput,
        include: Optional[types.FactInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Fact record.

        Parameters
        ----------
        where
            Fact filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Fact model

        Returns
        -------
        prisma.models.Fact
            The deleted Fact record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fact = Fact.prisma().delete(
            where={
                'Fact_id': 'idghgaicb',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.FactWhereUniqueInput,
        include: Optional[types.FactInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Fact record.

        Parameters
        ----------
        where
            Fact filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Fact model

        Returns
        -------
        prisma.models.Fact
            The found Fact record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fact = Fact.prisma().find_unique(
            where={
                'Fact_id': 'fjfddhigg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.FactWhereUniqueInput,
        include: Optional[types.FactInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Fact record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Fact filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Fact model

        Returns
        -------
        prisma.models.Fact
            The found Fact record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fact = Fact.prisma().find_unique_or_raise(
            where={
                'Fact_id': 'hjaecfifb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FactWhereInput] = None,
        cursor: Optional[types.FactWhereUniqueInput] = None,
        include: Optional[types.FactInclude] = None,
        order: Optional[Union[types.FactOrderByInput, List[types.FactOrderByInput]]] = None,
        distinct: Optional[List[types.FactScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Fact records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Fact records returned
        skip
            Ignore the first N results
        where
            Fact filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Fact model
        order
            Order the returned Fact records by any field
        distinct
            Filter Fact records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Fact]
            The list of all Fact records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Fact records
        facts = Fact.prisma().find_many(take=10)

        # find the first 5 Fact records ordered by the num_views field
        facts = Fact.prisma().find_many(
            take=5,
            order={
                'num_views': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FactWhereInput] = None,
        cursor: Optional[types.FactWhereUniqueInput] = None,
        include: Optional[types.FactInclude] = None,
        order: Optional[Union[types.FactOrderByInput, List[types.FactOrderByInput]]] = None,
        distinct: Optional[List[types.FactScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Fact record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Fact filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Fact model
        order
            Order the returned Fact records by any field
        distinct
            Filter Fact records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Fact
            The first Fact record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Fact record ordered by the Fact_id field
        fact = Fact.prisma().find_first(
            skip=1,
            order={
                'Fact_id': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FactWhereInput] = None,
        cursor: Optional[types.FactWhereUniqueInput] = None,
        include: Optional[types.FactInclude] = None,
        order: Optional[Union[types.FactOrderByInput, List[types.FactOrderByInput]]] = None,
        distinct: Optional[List[types.FactScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Fact record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Fact filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Fact model
        order
            Order the returned Fact records by any field
        distinct
            Filter Fact records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Fact
            The first Fact record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Fact record ordered by the Fact field
        fact = Fact.prisma().find_first_or_raise(
            skip=1,
            order={
                'Fact': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.FactUpdateInput,
        where: types.FactWhereUniqueInput,
        include: Optional[types.FactInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Fact record.

        Parameters
        ----------
        data
            Fact record data specifying what to update
        where
            Fact filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Fact model

        Returns
        -------
        prisma.models.Fact
            The updated Fact record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        fact = Fact.prisma().update(
            where={
                'Fact_id': 'cbbbjbfcii',
            },
            data={
                # data to update the Fact record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.FactWhereUniqueInput,
        data: types.FactUpsertInput,
        include: Optional[types.FactInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Fact filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Fact model

        Returns
        -------
        prisma.models.Fact
            The created or updated Fact record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fact = Fact.prisma().upsert(
            where={
                'Fact_id': 'bbejhfidcb',
            },
            data={
                'create': {
                    'Fact_id': 'bbejhfidcb',
                    'Fact': 'cghideieh',
                    'num_views': 180171308,
                },
                'update': {
                    'Fact': 'cghideieh',
                    'num_views': 180171308,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.FactUpdateManyMutationInput,
        where: types.FactWhereInput,
    ) -> int:
        """Update multiple Fact records

        Parameters
        ----------
        data
            Fact data to update the selected Fact records to
        where
            Filter to select the Fact records to update

        Returns
        -------
        int
            The total number of Fact records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Fact records
        total = Fact.prisma().update_many(
            data={
                'num_views': 1644289366
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FactWhereInput] = None,
        cursor: Optional[types.FactWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Fact records present in the database

        Parameters
        ----------
        select
            Select the Fact fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Fact filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FactCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Fact.prisma().count()

        # results: prisma.types.FactCountAggregateOutput
        results = Fact.prisma().count(
            select={
                '_all': True,
                'Fact_id': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.FactCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FactWhereInput] = None,
        cursor: Optional[types.FactWhereUniqueInput] = None,
    ) -> types.FactCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.FactCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FactWhereInput] = None,
        cursor: Optional[types.FactWhereUniqueInput] = None,
    ) -> Union[int, types.FactCountAggregateOutput]:
        """Count the number of Fact records present in the database

        Parameters
        ----------
        select
            Select the Fact fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Fact filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FactCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Fact.prisma().count()

        # results: prisma.types.FactCountAggregateOutput
        results = Fact.prisma().count(
            select={
                '_all': True,
                'Fact': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FactCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.FactWhereInput] = None
    ) -> int:
        """Delete multiple Fact records.

        Parameters
        ----------
        where
            Optional Fact filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Fact records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Fact records
        total = Fact.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.FactScalarFieldKeys'],
        *,
        where: Optional['types.FactWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FactAvgAggregateInput'] = None,
        sum: Optional['types.FactSumAggregateInput'] = None,
        min: Optional['types.FactMinAggregateInput'] = None,
        max: Optional['types.FactMaxAggregateInput'] = None,
        having: Optional['types.FactScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FactCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FactScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FactScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FactGroupByOutput']:
        """Group Fact records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Fact fields to group records by
        where
            Fact filter to select records
        take
            Limit the maximum number of Fact records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FactGroupByOutput]
            A list of dictionaries representing the Fact record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Fact records by num_views values
        # and count how many records are in each group
        results = Fact.prisma().group_by(
            ['num_views'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models